<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark static-top mb-3">
  <div class="container-fluid mx-3">
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path %>">
      <%= image_tag("markus-bikes.png", style: 'height:36px') %>
      Markus's Bikes
        </a>
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <%= link_to "List my orders", bike_orders_path, class: "nav-item nav-link" %>
        <%= link_to "New order", new_bike_order_path, class: "nav-item nav-link" %>
      </ul>

      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if user_signed_in? %>
            <div class="nav-item">
                <%= current_user.username %>
            </div>
            <%= link_to 'Sign Out', destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
        <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "nav-item nav-link" %>
            <%= link_to "Login", new_user_session_path, class: "nav-item nav-link" %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>